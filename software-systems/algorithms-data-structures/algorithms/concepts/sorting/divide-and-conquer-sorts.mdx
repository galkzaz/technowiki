---
id: divide-and-conquer-sorts
title: Divide-and-Conquer Sorts
description: Divide-and-Conquer Sorts
---

**Divide-and-Conquer (D&C)** is a problem-solving paradigm used heavily in sorting.

**Divide-and-Conquer sorting** is a general paradigm that divides a problem, solves subproblems, and combines results.

**Core structure:**
```
Divide → Conquer → Combine
```
Meaning:
1. **Divide**: Break the array into smaller subarrays
2. **Conquer**: Recursively sort those subarrays
3. **Combine**: Merge results into a final sorted array

Two ways to "divide" in D&C sorting:

1. **Structural Division (Merge Sort)**
    - Divide by index (middle)
    - Data is not rearranged initially
    - Fix happens in combine phase

2. **Value-Based Division (Partitioning)**
    - Divide by value (pivot)
    - Data is rearranged immediately
    - No combine phase needed

We can classify sorting algorithms:
```
Divide-and-Conquer Sorts
│
├── Merge-Based (need combine step)
│   └── Merge Sort
│
└── Partition-Based (no combine step)
    └── Quick Sort
```

## Partition-Based Sorting(Value-Based Division)

**(Partition-Based Sorting** is a core algorithmic paradigm where an array is rearranged (partitioned) around a **pivot** element, and then sorting is achieved by recursively applying the same idea to subarrays.

This idea is most famously used in **Quicksort**, but the partitioning technique itself appears in many algorithms (selection, median finding, etc.).

### Core Idea
A Divide and Conquer strategy solves a problem by dividing it into two independent subproblems, each about half the size of the original problem. You can apply this strategy to sorting as follows: 
1. find the median element in the collection `A` and swap it with the middle element of A. 
2. Now swap elements in the left half that are greater than `A[mid]` with elements in the right half that are less than or equal to `A[mid]`.
3. This subdivides the original array into two distinct subarrays that can be recursively sorted in place to sort the original collection `A`

The fundamental operation is: Partition the array so that:
- `Elements < pivot` go to the left
- `Elements > pivot` go to the right
- Pivot ends up in its final sorted position

After one partition step:
```
[ smaller elements | pivot | larger elements ]
```
Then recursively:
- Sort left part
- Sort right part

{/*A **partition-based sort** is a **Divide-and-Conquer** algorithm where the divide step rearranges the array such that no combine step is needed.*/}
A **partition-based sort** is a specialized form of **Divide-and-Conquer** where the division step rearranges the array around a pivot, eliminating the need for a combine step.

The Combine Step is the Key Differentiator

In Merge Sort:
- Heavy combine step
- Must merge arrays → O(n)

But in Quick Sort:
- No combine step
- Work is done during partition

This leads to the key classification: Partition-Based Sorting (Special Case of D&C)

How Quick Sort Fits: Step-by-step:
1. Partition
    ```
    [ elements < pivot | pivot | elements > pivot ]
    ```
2. Pivot is already in final position
3. **Recursively sort**:
    - Left subarray
    - Right subarray
4. No merge required

Partition-based sorting introduces a powerful reusable pattern: Partition Pattern:
- Choose pivot
- Rearrange elements
- Return pivot index

**Why Partition-Based Sorting is Powerful**
1. **In-place**: No extra memory (O(1) space)
2. **Cache-friendly**: Works on contiguous arrays
3. **Divide-and-Conquer**: Breaks problem recursively

Partition-based sorting works because: Each partition step places at least one element in its final position, This guarantees progress toward a fully sorted array.

**Why Partition-Based Sorting Works**

Key invariant after partition:
```
Left side ≤ pivot ≤ Right side
```
This guarantees:
- Pivot is in final sorted position
- Subproblems are independent

Used in:
- Quick Sort
- Quickselect
- Median of medians
- Dutch National Flag

### Computing the median(Pivot)
{/*Why not always pick the median as pivot?*/}
Implementing this approach is challenging because it might not be obvious how to compute the median element of a collection without sorting the collection first(The true median requires sorting (or near-sorting))!,That would cost $O(n log n)$ — defeating the purpose.

So we face a paradox:
- We want the median to get perfect balance
- But finding it is expensive

It turns out that you can use any element in `A` to partition `A` into two subarrays; if you
choose “wisely” each time, then both subarrays will be more or less the same size
and you will achieve an efficient implementation.

So, You don’t need the exact median — just a good enough pivot, Instead of perfect balance:
```
50% | pivot | 50%
```
We accept approximate balance:
```
30% | pivot | 70%
```
This is still efficient.

**Why “Any Element” Works**

You can pick any element as pivot:
- First element
- Last element
- Random element

Partition still works:
```
[ < pivot | pivot | > pivot ]
```
- Correctness is always preserved
- Performance depends on pivot quality

“Choose Wisely” means: Try to avoid extremely unbalanced partitions, for example

- Bad Pivot (Worst Case)
    ```
    [1 2 3 4 5]
    ```
    pivot = 1
    ```
    → [] 1 [2 3 4 5]
    ```
    This gives:
    ```
    T(n) = T(n-1) + O(n) → O(n²)
    ```
2. Good Pivot (Balanced)

    pivot ≈ middle value
    ```
    → [smaller half] pivot [larger half]
    ```
    This gives:
    ```
    T(n) = 2T(n/2) + O(n) → O(n log n)
    ```

**Balanced Recursion Tree**
```
Level 0: n
Level 1: n/2 + n/2
Level 2: n/4 + n/4 + n/4 + n/4
...
```
Height ≈ log n

**Unbalanced Recursion Tree**
```
n → n-1 → n-2 → ...
```
Height ≈ n

The efficiency of partition-based sorting depends not on choosing the exact median, but on consistently choosing pivots that produce reasonably balanced partitions, ensuring logarithmic recursion depth.

#### Pivot Strategies
**A. Random Pivot**
```
pivot = arr[rand() % n];
```
- Avoids worst case (with high probability)
- Used in practice

**B. Median-of-Three**

Pick median of: first, middle, last
- Very common optimization
- Good balance in real data

**C. True Median (Theoretical)**

Using Median of Medians algorithm:
- Guarantees perfect balance
- Runs in O(n)

BUT:
- High constant cost
- Rarely used in practice

### Partition Schemes(Partition Operation)
Partitioning is in-place and linear time O(n).

There are two classic schemes:

#### Lomuto Partition Scheme
The Lomuto partition scheme is a simple, efficient algorithm for Quicksort that reorders an array around a pivot—typically the last element—by scanning it once. It uses a "wall" pointer to track elements smaller than the pivot, swapping them to the front to ensure all smaller elements are left of the pivot.

Core Idea
- Pick last element as pivot
- Maintain boundary of smaller elements

We maintain a boundary:
```
i = end of "≤ pivot" region
```
Then scan with another pointer j.

**Key Aspects of the Lomuto Partition Scheme**
- **Pivot Selection**: Usually the last element in the given subarray is chosen as the pivot.
- Mechanism: Maintains a pointer `i` as a "wall" (initially before the first element) and iterates through the array with another pointer `j`.
- **Operation**: If an element `arr[j]` is less than or equal to the pivot, the wall `i` is moved forward, and arr[j] is swapped with `arr[i]`.
- **Final Step**: After the loop, the pivot (at the end) is swapped with the element at `i+1`, placing the pivot in its final sorted position.
- **Performance**: It is easy to implement but generally less efficient than Hoare’s partition scheme, performing more swaps. The time complexity is $O(n^2)$ in the worst case (e.g., sorted array) and $O(nlogn)$ in the average case.

Example Algorithm Steps
1. Initialize pivot as `arr[high]`.
2. Set a variable `i = low - 1` (the boundary for smaller elements).
3. **Iterate `j` from `low` to `high - 1`**:
    ◦ If `arr[j] <= pivot`, increment `i` and swap `arr[i]` with `arr[j]`.
4. Swap `arr[i + 1]` with `arr[high]` (place pivot in its place).
5. Return `i + 1`

Why i = left - 1? Because:
- It represents empty "≤ pivot" region 
- First valid element goes to left

**Visual Summary**

Before:
```
[ ? ? ? ? pivot ]
```
After:
```
[ ≤ pivot | pivot | > pivot ]
```

**Loop Invariant**

At any point during the loop:
```
A[left ... i]      ≤ pivot
A[i+1 ... j-1]     > pivot
A[j ... right-1]   unknown
```
This invariant is the heart of correctness

**Iteration Logic**

For each element A[j]:

1. **Case 1**: `A[j] ≤ pivot`
- Expand smaller region
- Swap into correct place
```
i++
swap(A[i], A[j])
```
2. **Case 2**: `A[j] > pivot`
- Do nothing
- Let it stay in "greater" region

Final Step

After loop:
```
swap(A[i+1], A[right])
```
This places pivot in its final sorted position.

##### Example Walkthrough
Input:
```
A = [8, 3, 5, 2, 7]
```
pivot = 7

Iterations:
| j | A[j] | Action | Array State     |
| - | ---- | ------ | --------------- |
| 0 | 8    | skip   | [8, 3, 5, 2, 7] |
| 1 | 3    | swap   | [3, 8, 5, 2, 7] |
| 2 | 5    | swap   | [3, 5, 8, 2, 7] |
| 3 | 2    | swap   | [3, 5, 2, 8, 7] |

Final swap:
```
swap(A[i+1], pivot)
```
→ [3, 5, 2, 7, 8]
Result:
```
[3, 5, 2] 7 [8]
        ↑
        p = 3
```

##### Pseudocode

Given an array segment:
```
A[left ... right]
```
We want to:
- Choose a pivot (usually `A[right]`) 
- Rearrange the array so that: 
```
A[left ... p-1] ≤ pivot
A[p]            = pivot
A[p+1 ... right] > pivot
```

<Tabs>

<TabItem value="Ps1" label="Pseudocode1" default>

```
function lomuto_partition(A, left, right):

    pivot ← A[right]
    i ← left - 1

    for j ← left to right - 1:
        if A[j] ≤ pivot:
            i ← i + 1
            swap A[i] ↔ A[j]

    swap A[i + 1] ↔ A[right]

    return i + 1
```

</TabItem>

<TabItem value="Pseudocode2" label="Pseudocode2">

```
// Divides array into three partitions
algorithm partition(A, lo, hi) is
  // Pivot value
  pivot := A[(lo + hi) / 2] // Choose the middle element as the pivot (integer division)
  
  // Lesser, equal and greater index
  lt := lo
  eq := lo
  gt := hi
  
  // Iterate and compare all elements with the pivot
  while eq <= gt do
    if A[eq] < pivot then
      // Swap the elements at the equal and lesser indices
      swap A[eq] with A[lt]
      // Increase lesser index
      lt := lt + 1
      // Increase equal index
      eq := eq + 1
    else if A[eq] > pivot then
      // Swap the elements at the equal and greater indices
      swap A[eq] with A[gt]
      // Decrease greater index
      gt := gt - 1
    else // if A[eq] = pivot then
      // Increase equal index
      eq := eq + 1
  
  // Return lesser and greater indices
  return lt, gt
```

</TabItem>
</Tabs>

##### Implementaions
<Tabs
  defaultValue='c'
  values={[
    {label: 'C', value: 'c'},
    {label: 'C++', value: 'cpp'},
    {label: 'Java', value: 'java'},
    {label: 'JavaScript', value: 'js'},
    {label: 'Rust', value: 'rust'},
  ]}
>

<TabItem value='c'>

  ```c
  #include <stdio.h>

  // Function to swap two elements
  void swap(int* a, int* b) {
      int temp = *a;
      *a = *b;
      *b = temp;
  }

  /*
   * Lomuto Partition Scheme:
   * 1. Uses the last element as the pivot.
   * 2. Maintains an index 'i' as the wall for elements <= pivot.
   * 3. Scans through the array with 'j' and swaps elements into the left partition.
   */
  int lomuto_partition(int arr[], int low, int high) {
      int pivot = arr[high];
      int i = (low - 1);

      for (int j = low; j < high; j++) {
          if (arr[j] <= pivot) {
              i++;
              swap(&arr[i], &arr[j]);
          }
      }
      swap(&arr[i + 1], &arr[high]);
      return (i + 1);
  }

  int main() {
      int arr[] = {10, 80, 30, 90, 40, 50, 70};
      int n = sizeof(arr) / sizeof(arr[0]);
      int pivot_index = lomuto_partition(arr, 0, n - 1);

      printf("Pivot placed at index: %d\n", pivot_index);
      printf("Partitioned array: ");
      for (int k = 0; k < n; k++) printf("%d ", arr[k]);
      return 0;
  }
  ```

  **Notes:**
  - This scheme is simpler to implement than Hoare's partition but is generally less efficient as it performs more swaps.
  - The function returns the index of the pivot in its final, sorted position.
  - Worst-case time complexity is $O(n^2)$ if the array is already sorted and the last element is chosen as the pivot.

</TabItem>

<TabItem value='cpp'>

  ```cpp
  #include <iostream>
  #include <vector>
  #include <algorithm>

  /**
   * Lomuto Partition Scheme:
   * Reorders the vector so all elements <= pivot are to its left and 
   * all elements > pivot are to its right.
   */
  int lomutoPartition(std::vector<int>& arr, int low, int high) {
      int pivot = arr[high];
      int i = low - 1;

      for (int j = low; j < high; j++) {
          if (arr[j] <= pivot) {
              i++;
              std::swap(arr[i], arr[j]);
          }
      }
      std::swap(arr[i + 1], arr[high]);
      return i + 1;
  }

  int main() {
      std::vector<int> arr = {10, 80, 30, 90, 40, 50, 70};
      int pIdx = lomutoPartition(arr, 0, arr.size() - 1);

      std::cout << "Pivot index: " << pIdx << "\nArray state: ";
      for (int x : arr) std::cout << x << " ";
      return 0;
  }
  ```

  **Notes:**
  - Uses the standard `std::swap` for concise code.
  - The logic ensures that after the call, the pivot is at `arr[i + 1]`.

</TabItem>

<TabItem value='java'>

  ```java
  import java.util.Arrays;

  public class LomutoPartition {

      /**
       * Lomuto Partition Scheme:
       * Scans the array and uses 'i' as a boundary for elements smaller than 
       * or equal to the pivot (the last element).
       */
      public static int lomutoPartition(int[] arr, int low, int high) {
          int pivot = arr[high];
          int i = low - 1;

          for (int j = low; j < high; j++) {
              if (arr[j] <= pivot) {
                  i++;
                  // Manual swap
                  int temp = arr[i];
                  arr[i] = arr[j];
                  arr[j] = temp;
              }
          }
          // Place pivot in correct position
          int temp = arr[i + 1];
          arr[i + 1] = arr[high];
          arr[high] = temp;

          return i + 1;
      }

      public static void main(String[] args) {
          int[] data = {10, 80, 30, 90, 40, 50, 70};
          int pivotIdx = lomutoPartition(data, 0, data.length - 1);

          System.out.println("Pivot index: " + pivotIdx);
          System.out.println("Array after partitioning: " + Arrays.toString(data));
      }
  }
  ```

  **Notes:**
  - Java uses 0-based indexing; `low - 1` initializes the boundary pointer outside the current range.
  - Time Complexity: $O(n)$ where $n$ is the number of elements in the range.

</TabItem>

<TabItem value='js'>

  ```javascript
  /**
   * Lomuto Partitioning:
   * Rearranges elements in the array based on a pivot (last element).
   * Elements smaller than the pivot are moved to the left.
   */
  function lomutoPartition(arr, low, high) {
    const pivot = arr[high];
    let i = low - 1;

    for (let j = low; j < high; j++) {
      if (arr[j] <= pivot) {
        i++;
        // Swap using array destructuring
        [arr[i], arr[j]] = [arr[j], arr[i]];
      }
    }

    // Final swap to place pivot correctly
    [arr[i + 1], arr[high]] = [arr[high], arr[i + 1]];
    return i + 1;
  }

  // Testing the method
  const data = [10, 80, 30, 90, 40, 50, 70];
  const pIndex = lomutoPartition(data, 0, data.length - 1);
  console.log(`Pivot index: ${pIndex}`);
  console.log(`Partitioned array: ${data}`);
  ```

  **Notes:**
  - JavaScript's array destructuring `[a, b] = [b, a]` provides a clean, modern way to swap elements.
  - The function modifies the array in-place, which is memory-efficient.

</TabItem>

<TabItem value='rust'>

  ```rust
  /**
   * Lomuto Partition Scheme:
   * Partitions a mutable slice of integers around its last element.
   */
  fn lomuto_partition(arr: &mut [i32], low: usize, high: usize) -> usize {
      let pivot = arr[high];
      let mut i = low as isize - 1;

      for j in low..high {
          if arr[j] <= pivot {
              i += 1;
              arr.swap(i as usize, j);
          }
      }

      let pivot_index = (i + 1) as usize;
      arr.swap(pivot_index, high);
      pivot_index
  }

  fn main() {
      let mut data = vec![10, 80, 30, 90, 40, 50, 70];
      let n = data.len();
      let p_idx = lomuto_partition(&mut data, 0, n - 1);

      println!("Pivot index: {}", p_idx);
      println!("Array after partition: {:?}", data);
  }
  ```

  **Notes:**
  - Rust uses the `swap` method on slices for safe and efficient element exchange.
  - The boundary pointer `i` is cast to `isize` to handle the negative starting index.

</TabItem>

</Tabs> 
C Code
```c
int partition(int arr[], int low, int high) {
    int pivot = arr[high];
    int i = low - 1;
    for (int j = low; j < high; j++) {
        if (arr[j] <= pivot) {
            i++;
            int temp = arr[i];
            arr[i] = arr[j];
            arr[j] = temp;
        }
    }
    // place pivot correctly
    int temp = arr[i + 1];
    arr[i + 1] = arr[high];
    arr[high] = temp;
    return i + 1;
}
```
    
Example Walkthrough
```
Initial array: [ 10 30 40 50 70 90 80 ]
Pivot = A[6] = 80

Step j = 0
Before: i = -1, j = 0, A[j] = 10
[ 10 30 40 50 70 90 80 ]
Action: A[j] <= pivot → increment i → i = 0
Swapping A[0]=10 and A[0]=10
After swap: [ 10 30 40 50 70 90 80 ]
-----------------------------------
Step j = 1
Before: i = 0, j = 1, A[j] = 30
[ 10 30 40 50 70 90 80 ]
Action: A[j] <= pivot → increment i → i = 1
Swapping A[1]=30 and A[1]=30
After swap: [ 10 30 40 50 70 90 80 ]
-----------------------------------
Step j = 2
Before: i = 1, j = 2, A[j] = 40
[ 10 30 40 50 70 90 80 ]
Action: A[j] <= pivot → increment i → i = 2
Swapping A[2]=40 and A[2]=40
After swap: [ 10 30 40 50 70 90 80 ]
-----------------------------------
Step j = 3
Before: i = 2, j = 3, A[j] = 50
[ 10 30 40 50 70 90 80 ]
Action: A[j] <= pivot → increment i → i = 3
Swapping A[3]=50 and A[3]=50
After swap: [ 10 30 40 50 70 90 80 ]
-----------------------------------
Step j = 4
Before: i = 3, j = 4, A[j] = 70
[ 10 30 40 50 70 90 80 ]
Action: A[j] <= pivot → increment i → i = 4
Swapping A[4]=70 and A[4]=70
After swap: [ 10 30 40 50 70 90 80 ]
-----------------------------------
Step j = 5
Before: i = 4, j = 5, A[j] = 90
[ 10 30 40 50 70 90 80 ]
Action: A[j] > pivot → no swap
-----------------------------------
Final step: place pivot in correct position
Swapping A[5]=90 and A[6]=80 (pivot)
Final array: [ 10 30 40 50 70 80 90 ]
Pivot index = 5
```
You Should Observe

1. Region Growth
    - i tracks end of ≤ pivot region 
    - This region grows step by step 

2. Invariant in Action

    At every step:
    ```
    A[left ... i]      ≤ pivot
    A[i+1 ... j-1]     > pivot
    ```
    You can literally see this in the output

3. Final Pivot Placement
    ```
    swap(A[i+1], A[right])
    ```
    This is the moment pivot becomes fixed

**Key Properties**

Simple
- Easy to implement 
- Easy to reason about 

More swaps
- Even when not necessary 

Not optimal for duplicates
- Can degrade performance

Lomuto partition works by growing a "≤ pivot" region from left to right, placing each qualifying element into its correct relative position in a single pass.

Compared to Hoare’s scheme, Lomuto is often preferred for educational purposes due to its straightforward logic

#### Hoare Partition Scheme
**Hoare's Partition Scheme** is a linear-time (O(n)) algorithm used in Quicksort to divide an array into two subarrays based on a "pivot" value. Invented by Sir Tony Hoare, it is generally more efficient than the Lomuto Partition Scheme because it performs roughly three times fewer swaps on average and handles duplicate elements more effectively

**Core Idea**

Instead of growing a “≤ pivot” region like Lomuto, Hoare uses: Two pointers moving toward each other
1. i → from left
2. j ← from right

They scan and swap misplaced elements.

Given an array `arr[]`, the task is to partition the array by **assuming first element as pivot element**. 

The partition of an array must satisfy the following two conditions:
- Elements smaller than pivot element must appear at index less than or equal to **partition index**.
- Elements larger than or equal to pivot element must appear at index greater than **partition index**.

Partition index is equal to count of elements, strictly smaller than pivot, minus one.

There might me more than one possible partition arrays.

**Examples:**

- Input:` arr[] = [5, 3, 8, 4, 2, 7, 1, 10]`
- Output: `[1, 3, 2, 4, 8, 7, 5, 10]`
- Explanation: The partition index is `3` and pivot element is `5`, all elements smaller than pivot element `[1, 3, 2, 4]`- were arranged before partition index and elements larger than or equal to pivot `[8, 7, 5, 10]` were arranged after- partition index.

- Input: `arr[] = [12, 10, 9, 16, 19, 9]`
- Output: `[9, 10, 9, 16, 19, 12]` 
- Explanation: The partition index is `2` and pivot element is `12`, all elements smaller than pivot element `[9, 10, 9]` were- arranged before or at partition index and elements larger than or equal to pivot `[16, 19, 12]` were arranged after partition index.

**How It Works**

We are fixing two types of mistakes:
1. Left side should have ≤ pivot
2. Right side should have ≥ pivot


Loop Behavior

1. **Step 1**: Move i right

- Find first element ≥ pivot
- This element is on the wrong side

2. **Step 2**: Move j left
- Find first element ≤ pivot
- Also on the wrong side

3. **Step 3**: Swap them
- Fix both mistakes in one swap

##### Example Walkthrough
Input:
```
A = [8, 3, 5, 2, 7]
pivot = A[left] = 8
```
Initial:
```
left = 0, right = 4

i = left - 1 = -1
j = right + 1 = 5
```
**Iteration 1**
- Move i right (find ≥ pivot)
```
i moves:
i = 0 → A[0] = 8 ≥ pivot → STOP
```
- Move j left (find ≤ pivot)
```
j moves:
j = 4 → A[4] = 7 ≤ pivot → STOP
```
- BEFORE SWAP
```
Index:   0   1   2   3   4
Array:  [8   3   5   2   7]
          ↑               ↑
          i               j
```
A[i] = 8 (too big for left side)
A[j] = 7 (belongs to left side)
- SWAP A[i] ↔ A[j]
```
Array:  [7   3   5   2   8]
```
- AFTER SWAP
```
Index:   0   1   2   3   4
Array:  [7   3   5   2   8]
          ↑               ↑
          i               j
```
**Iteration 2**
- Move i right
```
i = 1 → A[1] = 3 < pivot → continue
i = 2 → A[2] = 5 < pivot → continue
i = 3 → A[3] = 2 < pivot → continue
i = 4 → A[4] = 8 ≥ pivot → STOP
```
- Move j left
```
j = 3 → A[3] = 2 ≤ pivot → STOP
```
- CURRENT STATE (Before check)
```
Index:   0   1   2   3   4
Array:  [7   3   5   2   8]
                          ↑
                          i
                      ↑
                      j
```
- Check condition
```
i = 4, j = 3 → i ≥ j → STOP
```

Final Result

Partition returns:

```
p = j = 3
- Final Partitioned Array
[7   3   5   2 | 8]
                ^
                split
```
Key Observations
- Left side:
[7, 3, 5, 2] ≤ pivot (8)
- Right side:
[8] ≥ pivot
- Pivot (8) ended up at the end by coincidence, not by guarantee.

Hoare only guarantees:
```
Left ≤ pivot   |   Right ≥ pivot
```
NOT: pivot is at final sorted position
##### Pseudocode
```
function hoare_partition(A, left, right):
    pivot ← A[left]
    i ← left - 1
    j ← right + 1

    while true:

        repeat:
            i ← i + 1
        until A[i] ≥ pivot

        repeat:
            j ← j - 1
        until A[j] ≤ pivot

        if i ≥ j:
            return j

        swap A[i] ↔ A[j]
```
Return Value Meaning
```
return j
```
This means:
```
A[left ... j]     ≤ pivot
A[j+1 ... right]  ≥ pivot
```
It returns a partition boundary, not pivot index

**Loop Invariant**

At any point:
```
A[left ... i-1]   ≤ pivot
A[j+1 ... right] ≥ pivot
```
Unknown region:
```
A[i ... j]
```
Visual Summary
```
Before:
[ ? ? ? ? ? ]

During:
[ ≤ pivot | unknown | ≥ pivot ]

After:
[ ≤ pivot | ≥ pivot ]
```
Hoare partition works by simultaneously correcting misplaced elements from both ends, leading to fewer swaps and better performance, but it does not place the pivot in its final sorted position.

**Common Pitfalls** 
- Assuming pivot ends in final position
- Using wrong recursion boundaries
- Infinite loop (wrong conditions)

##### Implementaions
<Tabs
  defaultValue='c'
  values={[
    {label: 'C', value: 'c'},
    {label: 'C++', value: 'cpp'},
    {label: 'Java', value: 'java'},
    {label: 'JavaScript', value: 'js'},
    {label: 'Rust', value: 'rust'},
  ]}
>

<TabItem value='c'>

  ```c
  #include <stdio.h>

  // Function to swap two elements
  void swap(int* a, int* b) {
      int temp = *a;
      *a = *b;
      *b = temp;
  }

  /*
   * Hoare Partition Scheme:
   * 1. Uses the first element as the pivot.
   * 2. Uses two pointers (i and j) that move toward each other.
   * 3. Swaps elements that are on the wrong side of the pivot.
   * 4. More efficient than Lomuto as it performs fewer swaps on average.
   */
  int hoare_partition(int arr[], int low, int high) {
      int pivot = arr[low];
      int i = low - 1;
      int j = high + 1;

      while (1) {
          // Increment i while elements are less than the pivot
          do {
              i++;
          } while (arr[i] < pivot);

          // Decrement j while elements are greater than the pivot
          do {
              j--;
          } while (arr[j] > pivot);

          // If pointers meet or cross, partitioning is complete
          if (i >= j) return j;

          // Swap elements to correct their positions relative to pivot
          swap(&arr[i], &arr[j]);
      }
  }

  int main() {
      int arr[] = {10, 80, 30, 90, 40, 50, 70};
      int n = sizeof(arr) / sizeof(arr[0]);
      int split_index = hoare_partition(arr, 0, n - 1);

      printf("Split index: %d\n", split_index);
      printf("Array after partitioning: ");
      for (int k = 0; k < n; k++) printf("%d ", arr[k]);
      return 0;
  }
  ```

  **Notes:**
  - The function returns the split index `j`, not necessarily the final position of the pivot.
  - The two subarrays for recursion would be `[low...j]` and `[j+1...high]`.
  - Average time complexity is $O(n)$, and it is generally faster than the Lomuto scheme.

</TabItem>

<TabItem value='cpp'>

  ```cpp
  #include <iostream>
  #include <vector>
  #include <algorithm>

  /**
   * Hoare Partition Scheme:
   * Efficiently reorders elements around a pivot.
   * Returns index 'j' such that arr[low..j] <= pivot and arr[j+1..high] >= pivot.
   */
  int hoarePartition(std::vector<int>& arr, int low, int high) {
      int pivot = arr[low];
      int i = low - 1;
      int j = high + 1;

      while (true) {
          do {
              i++;
          } while (arr[i] < pivot);

          do {
              j--;
          } while (arr[j] > pivot);

          if (i >= j) return j;
          std::swap(arr[i], arr[j]);
      }
  }

  int main() {
      std::vector<int> arr = {10, 80, 30, 90, 40, 50, 70};
      int splitIdx = hoarePartition(arr, 0, arr.size() - 1);

      std::cout << "Split index: " << splitIdx << "\nArray state: ";
      for (int x : arr) std::cout << x << " ";
      return 0;
  }
  ```

  **Notes:**
  - Uses `std::swap` for efficient element exchange.
  - Unlike Lomuto, Hoare's scheme works correctly even if the pivot is the smallest or largest element.
  - Memory usage is $O(1)$ extra space (in-place).

</TabItem>

<TabItem value='java'>

  ```java
  import java.util.Arrays;

  public class HoarePartition {

      /**
       * Hoare Partition Scheme:
       * Scans from both ends and swaps misplaced elements.
       * Uses the first element as the pivot by default.
       */
      public static int hoarePartition(int[] arr, int low, int high) {
          int pivot = arr[low];
          int i = low - 1;
          int j = high + 1;

          while (true) {
              // Move i right as long as arr[i] < pivot
              do {
                  i++;
              } while (arr[i] < pivot);

              // Move j left as long as arr[j] > pivot
              do {
                  j--;
              } while (arr[j] > pivot);

              // If indices cross, the split point is found
              if (i >= j) {
                  return j;
              }

              // Swap arr[i] and arr[j]
              int temp = arr[i];
              arr[i] = arr[j];
              arr[j] = temp;
          }
      }

      public static void main(String[] args) {
          int[] data = {10, 80, 30, 90, 40, 50, 70};
          int splitIdx = hoarePartition(data, 0, data.length - 1);

          System.out.println("Split index: " + splitIdx);
          System.out.println("Array after partitioning: " + Arrays.toString(data));
      }
  }
  ```

  **Notes:**
  - The `do-while` loops ensure that the pointers move at least once, avoiding infinite loops with duplicate elements.
  - Naming follows standard Java `camelCase`.
  - The split index `j` defines the two sub-problems for the Quicksort algorithm.

</TabItem>

<TabItem value='js'>

  ```javascript
  /**
   * Hoare Partitioning:
   * Rearranges the array elements using two pointers and a pivot.
   * Returns the split point index for recursive sorting.
   */
  function hoarePartition(arr, low, high) {
    const pivot = arr[low];
    let i = low - 1;
    let j = high + 1;

    while (true) {
      do {
        i++;
      } while (arr[i] < pivot);

      do {
        j--;
      } while (arr[j] > pivot);

      if (i >= j) return j;

      // Swap using destructuring assignment
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }

  // Testing the method
  const data = [10, 80, 30, 90, 40, 50, 70];
  const splitIndex = hoarePartition(data, 0, data.length - 1);
  console.log(`Split index: ${splitIndex}`);
  console.log(`Partitioned array: ${data}`);
  ```

  **Notes:**
  - Uses modern JavaScript destructuring for concise swapping logic.
  - The algorithm is in-place and highly efficient in terms of constant factors.
  - In Hoare's scheme, the pivot element can end up in either the left or right partition.

</TabItem>

<TabItem value='rust'>

  ```rust
  /**
   * Hoare Partition Scheme:
   * Partitions a mutable slice of i32 around its first element.
   * Returns the split point index 'j'.
   */
  fn hoare_partition(arr: &mut [i32], low: usize, high: usize) -> usize {
      let pivot = arr[low];
      let mut i = low as isize - 1;
      let mut j = high as isize + 1;

      loop {
          // Rust doesn't have do-while, so we use loop + break
          loop {
              i += 1;
              if arr[i as usize] >= pivot { break; }
          }
          loop {
              j -= 1;
              if arr[j as usize] <= pivot { break; }
          }

          if i >= j {
              return j as usize;
          }

          arr.swap(i as usize, j as usize);
      }
  }

  fn main() {
      let mut data = vec![10, 80, 30, 90, 40, 50, 70];
      let n = data.len();
      let split_idx = hoare_partition(&mut data, 0, n - 1);

      println!("Split index: {}", split_idx);
      println!("Array after partition: {:?}", data);
  }
  ```

  **Notes:**
  - Rust's `swap` method provides a safe way to exchange elements in a slice.
  - Pointers `i` and `j` are cast to `isize` to handle the initial boundary state safely.
  - The split point `j` is used to create sub-slices for recursive partitioning.

</TabItem>

</Tabs>
#### Key Difference between Lomuto and Hoare
| Feature        | Lomuto                 | Hoare                                        |
| -------------- | ---------------------- | -------------------------------------------- |
| Pivot position | Usually last element   | Usually first element                        |
| Pointers       | One (j) + boundary (i) | Two pointers (i, j)                          |
| Swaps          | More                   | Fewer                                        |
| Return value   | Final pivot index      | Split index (not necessarily pivot position) |

Lomuto:
- Easier 
- More “teaching-friendly” 
- Pivot ends in correct position 

Hoare:
- More efficient 
- More subtle 
- Used in high-performance implementations

Why Hoare is Faster
- Fewer swaps
    - Swaps only when necessary
    - Fixes two elements per swap
- Better cache behavior
    - Fewer writes
- Works well with duplicates (better than Lomuto)
#### Differnce between Pivot strategy and Partition scheme
**Pivot strategy** decides what to partition around **Partition scheme** decides how to do it efficiently.

Choosing A[p] (pivot) → belongs to Pivot Strategies,  Rearranging the array around pivot → belongs to Partition Schemes. Think of It Like This
1. **Step 1**: Choose pivot  ← (Pivot Strategy)
2. **Step 2**: Partition     ← (Partition Scheme)

Pivot Strategy (Selection Problem) answers: Which element should we use as pivot?
Examples:
- First element
- Last element
- Random element
- Median-of-three
- Median-of-medians

This affects performance (balance)

Partition Scheme (Rearrangement Problem) answers: How do we rearrange elements around the pivot?
Examples:
- Lomuto partition
- Hoare partition
- 3-way partition (Dutch National Flag)

This affects:
- Number of swaps
- Constant factors
- Stability behavior

Example Flow

Suppose: `A = [8, 3, 5, 2, 7]`

Step 1: Pivot Strategy

Pick pivot:
- pivot = A[2] = 5

Step 2: Partition Scheme

Apply (e.g., Lomuto or Hoare): → [3, 2] 5 [8, 7]

This Separation Matters, Because you can mix and match:
| Pivot Strategy | Partition Scheme |
| -------------- | ---------------- |
| Random pivot   | Lomuto           |
| Median-of-3    | Hoare            |
| First element  | 3-way partition  |

**Performance Impact**

Pivot Strategy → affects:
- Recursion depth
- Balance of subarrays
- Time complexity (O(n log n) vs O(n²))

Partition Scheme → affects:
- Number of swaps
- Cache behavior
- Constant factors (real-world speed)

**Analogy**

Think of sorting like organizing people:
- Pivot Strategy = choosing a person as a reference
- Partition Scheme = how you physically arrange people around them

Although separate, they interact:
- Some schemes assume pivot is at a specific position
(e.g., Lomuto uses last element)
- Sometimes you:
    1. Choose pivot
    2. Swap it into a convenient position
    3. Run partition

### Quick Sort
## Merge-Based Sorting(Structural Division)
### Merge Sort
If we have a big list to sort, we can split it into halves: each half list becomes a sorting subproblem. To sort the big list, subproblem solutions (i.e., the sorted halves) can be merged in a single list using the merge algorithm. But how will we sort our two subproblems? They are themselves split into subsubproblems, sorted and merged. The new subsubproblems will also be split, sorted, and merged. The splitting continues until we hit the base case: a one-item list. A one-item list is already sorted!

Merge Sort is the textbook example of
Divide and Conquer. It breaks a list into individual elements (Divide), sorts them by merging (Conquer), and builds the final sorted list (Combine).

Idea:
 - Split array into halves
 - Recursively sort
 - Merge sorted halves

All D&C sorts follow:
$$
 T(n) = aT(n/b) + f(n)
$$
Where:
- a = number of subproblems
- n/b = size of each
- f(n) = cost of divide + combine

Examples:
| Algorithm  | Recurrence             | Key Cost       |
| ---------- | ---------------------- | -------------- |
| Merge Sort | 2T(n/2) + O(n)         | Merge step     |
| Quick Sort | T(k) + T(n-k-1) + O(n) | Partition step |

### Core Idea (Divide and Conquer)

* Divide: Split the array into two halves
* Conquer: Recursively sort each half
* Combine: Merge the two sorted halves into a single sorted array

### Pseudocode
``` [Pseudocode]
function merge_sort(list)
  if list.length = 1
    return list
  left ← list.first_half
  right ← list.last_half
  return merge(merge_sort(left), merge_sort(right))
```
![Sample Merge Sort execution. Rectangles are individual merge_sort calls, with inputs on top and outputs at the bottom](/docs/assets/software-systems/data-structures-and-algorithms/algorithms/sorting/sorting-merging-example1.png)

### Visual recursion-tree explanation 
Think of Merge Sort as a tree growing downward, then collapsing upward.
1. Top level (original problem)
[8, 3, 5, 4, 7, 6, 1, 2]
2. Level 1
[8, 3, 5, 4]        [7, 6, 1, 2]
3. Level 2
[8, 3]   [5, 4]     [7, 6]   [1, 2]
4. Level 3 (leaves)
[8] [3] [5] [4]     [7] [6] [1] [2]

This is the divide phase — the tree grows downward.

Now the tree collapses upward:
- Leaves merge into size-2 arrays
- Size-2 arrays merge into size-4
- Size-4 arrays merge into size-8

Each level does:
-Linear work (merging)
- Over log n levels

That's where the famous O(n log n) comes from.

### Steps
**Phase 1: The 'Divide' Stage (Top-Down)**

The algorithm recursively splits the array in half until each sub-array contains only one element. A single element is considered 'sorted' by default.
|     |     |     |     |
| --- | --- | --- | --- |
| Level	 | Left Sub-array	 | Right Sub-array	 | Action |
| 1	 | [38, 27, 43, 3]	 | [9, 82, 10]	 | Split original array in half |
| 2 (Left)	 | [38, 27]	 | [43, 3]	 | Split the left half |
| 2 (Right)	 | [9, 82]	 | [10]	 | Split the right half |
| 3 (Final)	 | [38], [27], [43], [3]	 | [9], [82], [10]	 | All elements are now isolated |

**Step 2: Conquer (Sort Base Cases)**

Subarrays of length 1 are already sorted: [38], [27], [43], [3], [9], [82], [10]

Now, the algorithm merges the small pieces back together. During the merge, it compares the front elements of each list and places the smaller one first.

**Step 3: Merging individual elements into pairs**
|     |     |
| --- | --- |
| Merging Elements	 | Resulting Sorted Sub-array |
| [38] and [27]	 | [  27, 38] | 
|[43] and [3]	 | [3, 43] | 
|[9] and [82]	 | [9, 82] | 
|[10] and [10]	 | [10, 10] |

**Step 3: Merging pairs into larger blocks**

To merge [27, 38] and [3, 43], the engine compares 27 vs 3 (picks 3), then 27 vs 43 (picks 27), then 38 vs 43 (picks 38).
|     |     |
| --- | --- |
| Merging Sub-arrays	 | Resulting Sorted Sub-array |
| [27, 38] and [3, 43]	 | [3, 27, 38, 43] |
| [9, 82] and [10]	 | [9, 10, 82] |

**Step 4: The Final Merge (Global Solution)**

This final 'Combine' step merges the two remaining halves to produce the final sorted array.
|     |     |     |     |
| --- | --- | --- | --- |
| Left | Right Index	 | Comparison	 | Winner to Result Array |
| 27	 | 3	 | 3 &lt; 27	 | [3] |
| 27	 | 9	 | 9 &lt; 27	 | [3, 9] |
| 27	 | 10	 | 10 &lt; 27	 | [3, 9, 10] |
| 27	 | 82	 | 27 &lt; 82	 | [3, 9, 10, 27] |
| 38	 | 82	 | 38 &lt; 82	 | [3, 9, 10, 27, 38] |
| 43	 | 82	 | 43 &lt; 82	 | [3, 9, 10, 27, 38, 43] |
| End | 82	 | Remainder	 | [3, 9, 10, 27, 38, 43, 82] |

###  time complexity
To do this, 
1. **Counting operations**: we're first going to count the operations generated by each individual split step.
    * Say we have a big list of size n. When called, the `merge_sort` function performs the following operations:
        * Splits list in halves, which does not depend on list size: O(1),
        * A merge: we recall from sec. 3.1 that merge is O(n),
        * Two merge_sort recursive calls that are not counted(Operations performed by recursive calls are counted in the next split step!).
    * Since we keep the strongest term and don't count recursive calls, the time complexity of the function is O(n). 
    * Let's now count the time complexity of each split step:
        * Split step 1. The merge_sort function is called for a list of n items. The time complexity of this step is O(n).
        * Split step 2. The merge_sort function is called twice, each time for n/2 items. We find 2 × O(n/2) = O(n).
        * Split step 3. The merge_sort function is called four times, each time for n/4 items: 4 × O(n/4) = O(n).
        * Split step x. The merge_sort function is called 2x times, each for a list of n/2x items: 2x × O(n/2x) = O(n).
      * The split steps all have the same complexity of O(n). Merge Sort's time complexity is thus x × O(n), where x is the number of split steps necessary for its full execution(We can't ignore x because it's not a constant. If the list size n doubles, you'll need one more split step. If n quadruples, you'll need two more split steps.).
2. **Counting Steps**: Then, we'll count how many split steps there are in total.
    * How do we evaluate x? We know recursive functions stop to call themselves once they hit their base case. Our base
    case is a one-item list. We've also seen that split step x works on lists of n/2x items. Therefore:
      $$
      \frac{n}{2^x} = 1 → 2^x = n → x = \log_2 n.
      $$
    * Any process that reduces an input step-by-step, dividing it by a constant factor in each step, takes a logarithmic number of steps to fully reduce the input.

Merge Sort's time complexity is thus $(log_2 n \times O(n)) = O(n log n)$.

That's a huge improvement over the O(n2) Selection Sort. Do you
remember the performance gap between log-linear and quadratic
algorithms we've seen last chapter in fig. 2.4? Even if a faster computer crunches the O(n2) algorithm, it will end up slower than a computer crunching the O(n log n) algorithm:

See how slowly the number of required split steps increases12 with
the total number of items to sort:

| Input size (n) | log₂ n | Split steps required |
| --------------- | ------ | -------------------- |
| 10              | 3.32   | 4                    |
| 100             | 6.64   | 7                    |
| 1,024           | 10.00  | 10                   |
| 1,000,000       | 19.93  | 20                   |
| 1,000,000,000   | 29.89  | 30                   |

For big inputs, O(n log n) algorithms in slow computers are much faster than O(n2) algorithms in 1000× faster computers.
| Input Size | Quadratic | Log-linear |
| --- | --- | --- |
| 196 (countries in the world) | 38 ms | 2 s |
| 44K (airports in the world) | 32 min | 12 min |
| 171K (English dictionary words) | 8 hours | 51 min |
| 1M (inhabitants of Hawaii) | 12 days | 6 hours |
| 19M (inhabitants of Florida) | 11 years | 6 days |
| 130M (books ever published) | 500 years | 41 days |
| 4.7G (web pages of the Internet) | 700K years | 5 years |

### Space Complexity

### Code

<Tabs
  defaultValue='python'
  values={[
    {label: 'C', value: 'c'},
    {label: 'C++', value: 'cpp'},
    {label: 'Java', value: 'java'},
    {label: 'Python', value: 'python'},
    {label: 'Rust', value: 'rust'},
  ]}
>

<TabItem value='c'>

  ```c
  #include <stdio.h>

  void merge(int arr[], int l, int m, int r) {
      int n1 = m - l + 1;
      int n2 = r - m;

      int L[n1], R[n2];

      for (int i = 0; i < n1; i++)
          L[i] = arr[l + i];
      for (int j = 0; j < n2; j++)
          R[j] = arr[m + 1 + j];

      int i = 0, j = 0, k = l;

      while (i < n1 && j < n2) {
          if (L[i] <= R[j])
              arr[k++] = L[i++];
          else
              arr[k++] = R[j++];
      }

      while (i < n1)
          arr[k++] = L[i++];

      while (j < n2)
          arr[k++] = R[j++];
  }

  void mergeSort(int arr[], int l, int r) {
      if (l < r) {
          int m = l + (r - l) / 2;
          mergeSort(arr, l, m);
          mergeSort(arr, m + 1, r);
          merge(arr, l, m, r);
      }
  }

  int main() {
      int arr[] = {38, 27, 43, 3, 9, 82, 10};
      int n = sizeof(arr) / sizeof(arr[0]);

      mergeSort(arr, 0, n - 1);

      for (int i = 0; i < n; i++)
          printf('%d ', arr[i]);

      return 0;
  }
  ```

</TabItem>

<TabItem value='cpp'>

  ```cpp
  #include <iostream>
  #include <vector>
  using namespace std;

  void merge(vector<int>& arr, int l, int m, int r) {
      vector<int> L(arr.begin() + l, arr.begin() + m + 1);
      vector<int> R(arr.begin() + m + 1, arr.begin() + r + 1);

      int i = 0, j = 0, k = l;

      while (i < L.size() && j < R.size()) {
          if (L[i] <= R[j])
              arr[k++] = L[i++];
          else
              arr[k++] = R[j++];
      }

      while (i < L.size())
          arr[k++] = L[i++];

      while (j < R.size())
          arr[k++] = R[j++];
  }

  void mergeSort(vector<int>& arr, int l, int r) {
      if (l < r) {
          int m = l + (r - l) / 2;
          mergeSort(arr, l, m);
          mergeSort(arr, m + 1, r);
          merge(arr, l, m, r);
      }
  }

  int main() {
      vector<int> arr = {38, 27, 43, 3, 9, 82, 10};
      mergeSort(arr, 0, arr.size() - 1);

      for (int x : arr)
          cout << x << ' ';

      return 0;
  }
  ```

</TabItem>

<TabItem value='java'>

  ```java
  class MergeSort {

      void merge(int[] arr, int l, int m, int r) {
          int n1 = m - l + 1;
          int n2 = r - m;

          int[] L = new int[n1];
          int[] R = new int[n2];

          for (int i = 0; i < n1; i++)
              L[i] = arr[l + i];
          for (int j = 0; j < n2; j++)
              R[j] = arr[m + 1 + j];

          int i = 0, j = 0, k = l;

          while (i < n1 && j < n2) {
              if (L[i] <= R[j])
                  arr[k++] = L[i++];
              else
                  arr[k++] = R[j++];
          }

          while (i < n1)
              arr[k++] = L[i++];

          while (j < n2)
              arr[k++] = R[j++];
      }

      void mergeSort(int[] arr, int l, int r) {
          if (l < r) {
              int m = l + (r - l) / 2;
              mergeSort(arr, l, m);
              mergeSort(arr, m + 1, r);
              merge(arr, l, m, r);
          }
      }

      public static void main(String[] args) {
          int[] arr = {38, 27, 43, 3, 9, 82, 10};
          MergeSort ms = new MergeSort();
          ms.mergeSort(arr, 0, arr.length - 1);

          for (int x : arr)
              System.out.print(x + ' ');
      }
  }
  ```

</TabItem>

<TabItem value='python'>

  ```python
  def merge_sort(arr):
      if len(arr) <= 1:
          return arr

      mid = len(arr) // 2
      left = merge_sort(arr[:mid])
      right = merge_sort(arr[mid:])

      return merge(left, right)

  def merge(left, right):
      result = []
      i = j = 0

      while i < len(left) and j < len(right):
          if left[i] <= right[j]:
              result.append(left[i])
              i += 1
          else:
              result.append(right[j])
              j += 1

      result.extend(left[i:])
      result.extend(right[j:])
      return result

  arr = [38, 27, 43, 3, 9, 82, 10]
  sorted_arr = merge_sort(arr)
  print(sorted_arr)
  ```
</TabItem> 
<TabItem value='rust'>

  ```rust
  fn merge_sort(arr: &mut Vec<i32>) {
      let n = arr.len();
      if n <= 1 {
          return;
      }

      let mid = n / 2;
      let mut left = arr[..mid].to_vec();
      let mut right = arr[mid..].to_vec();

      merge_sort(&mut left);
      merge_sort(&mut right);

      merge(arr, &left, &right);
  }

  fn merge(arr: &mut Vec<i32>, left: &Vec<i32>, right: &Vec<i32>) {
      let mut i = 0;
      let mut j = 0;
      let mut k = 0;

      while i < left.len() && j < right.len() {
          if left[i] <= right[j] {
              arr[k] = left[i];
              i += 1;
          } else {
              arr[k] = right[j];
              j += 1;
          }
          k += 1;
      }

      while i < left.len() {
          arr[k] = left[i];
          i += 1;
          k += 1;
      }

      while j < right.len() {
          arr[k] = right[j];
          j += 1;
          k += 1;
      }
  }

  fn main() {
      let mut arr = vec![38, 27, 43, 3, 9, 82, 10];
      merge_sort(&mut arr);
      println!('{:?}', arr);
  }
  ```

</TabItem> 
</Tabs> 


## Compare Merge Sort vs Quick Sort
Merge Sort
- Splits the array evenly
- Does the hard work during merge
- Tree shape is predictable and balanced

Quick Sort
- Splits based on a pivot
- Does the hard work during partition
- Tree shape depends on how good your pivot is

Same Divide & Conquer family, very different personalities.
| Feature         | Merge Sort       | Quick Sort             |
| --------------- | ---------------- | ---------------------- |
| Paradigm        | Divide & Conquer | Divide & Conquer       |
| Division method | Split in halves  | Partition around pivot |
| Combine step    | Required (merge) | Not needed             |
| Space           | O(n)             | O(1)                   |
| Stability       | Stable           | Not stable             |
| Worst-case      | O(n log n)       | O(n²)                  |

### Merge Sort recursion tree
Example array:
[8, 3, 5, 4, 7, 6, 1, 2]
```
Divide phase (always balanced)
                [8 3 5 4 7 6 1 2]
               /                 \
        [8 3 5 4]               [7 6 1 2]
         /       \               /       \
     [8 3]      [5 4]         [7 6]      [1 2]
     /   \      /   \         /   \      /   \
   [8]  [3]   [5]  [4]      [7]  [6]   [1]  [2]
```
- Height of tree ≈ log n
- Every level processes all elements
- Shape never changes, no surprises

Combine phase (bottom-up)

Each level merges sorted arrays.

That's why:
- Work per level = O(n)
- Number of levels = O(log n)
- Total = O(n log n)

always Guaranteed.

### Quick Sort recursion tree (good pivot)
Same array, assume a good pivot (close to median).
```
                [8 3 5 4 7 6 1 2]
                      |
                    pivot = 5
                      |
          --------------------------------
          |                              |
      [3 4 1 2]                    [8 7 6]
           |                            |
        pivot = 3                   pivot = 7
           |                            |
     -----------                   ----------
     |         |                   |        |
   [1 2]      [4]                [6]       [8]
```
- Still roughly balanced
- Tree height ≈ log n
- Partitioning at each level costs O(n)
- So in the best/average case: Quick Sort = O(n log n)

### Quick Sort recursion tree (bad pivot)
Now imagine always picking the smallest or largest element as pivot.
```
[1 2 3 4 5 6 7 8]

pivot = 1

[ ]   [2 3 4 5 6 7 8]
        pivot = 2

[ ]   [3 4 5 6 7 8]
          pivot = 3
...
```
Tree becomes a chain:
```
height = n
work per level = n
total = O(n²)
```
This never happens to Merge Sort.

**Why Quick Sort is often faster in practice**

Even though Merge Sort has a guaranteed bound, Quick Sort often wins because:
- No extra merging arrays
- Better cache locality
- Less constant overhead
- Most pivots are "good enough"

It's a classic theoretical vs practical trade-off.

### Comparison Table
| Aspect       | Merge Sort         | Quick Sort           |
| ------------ | ------------------ | -------------------- |
| Split        | Always in half     | Based on pivot       |
| Tree shape   | Always balanced    | Depends on pivot     |
| Worst case   | O(n log n)         | O(n²)                |
| Average case | O(n log n)         | O(n log n)           |
| Extra memory | Needs extra arrays | In-place             |
| Stability    | Stable             | Not stable (usually) |

**Tree intuition summary**

Merge Sort: "Perfect tree every time. I'll pay the merge cost."

Quick Sort: "Let me gamble on a pivot. If I win, I'm fast. If I lose, it hurts."

That recursion tree picture explains everything about their behavior.
