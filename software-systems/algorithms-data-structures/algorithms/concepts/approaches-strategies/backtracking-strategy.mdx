---
id: backtracking-strategy
title: Backtracking Strategy
description: Backtracking Strategy
sidebar_position: 10
---

**Backtracking** is a systematic way to explore the search space of possible solutions while pruning choices that cannot lead to a valid or optimal solution.

**Backtracking** is an algorithmic strategy for systematically exploring the solution space of a problem by incrementally constructing candidates and abandoning those that violate problem constraints. It is primarily used for problems in which solutions must satisfy a set of conditions, and the search space is combinatorial in nature.

**Backtracking** is an algorithmic strategy that enhances exhaustive search by pruning infeasible solutions early. It relies heavily on recursion and depth-first search, and it is particularly effective for problems involving constraints and combinatorial structures.

Backtracking is an algorithmic strategy used to solve problems by:

1. Building a solution step-by-step
2. Checking whether the current partial solution is still valid
3. Abandoning (“backtracking from”) paths that cannot lead to a valid solution
4. Trying alternative choices

It is essentially:

* **Depth-First Search (DFS)** over a decision tree
* With **pruning** of invalid or useless paths

At its core, **backtracking** is controlled **brute force**. It explores all possible solutions by making choices and then backtracking when a choice leads to an invalid or suboptimal solution.

**Backtracking** works best in problems where the solution is a sequence of choices and making a choice restrains subsequent choices.
It identifies as soon as possible the choices you've made cannot give you the solution you want, so you can sooner step back and try something else. Fail early, fail often.

At each step, the algorithm makes a tentative decision, extends a partial solution, and checks whether the current state remains feasible. If a partial solution cannot lead to a valid complete solution, the algorithm reverses (or backtracks) to a previous decision point and explores alternative choices.

## Core Idea

At every step:

1. Choose an option
2. Add it to the current state
3. Recurse
4. Undo the choice (backtrack)
5. Try the next option

## Mental Model

Imagine solving a maze:

* Walk forward
* If you hit a dead end:

  * go back
  * try another path

That “go back and try again” behavior is backtracking.

## Generic Structure

```text
backtrack(state):

    if solution found:
        save/output solution
        return

    for every possible choice:
        if choice is valid:

            make choice

            backtrack(new_state)

            undo choice
```

## Important Characteristics

| Feature                  | Description                               |
| ------------------------ | ----------------------------------------- |
| Incremental construction | Build solution piece by piece             |
| Recursive exploration    | Explore possibilities recursively         |
| Pruning                  | Stop exploring invalid paths early        |
| Undo operation           | Remove previous choice before trying next |
| DFS traversal            | Naturally follows depth-first search      |

## Key Components

### 1. State

Represents current partial solution.

Examples:

* Current subset
* Current path
* Current board configuration
* Current string permutation

---

### 2. Choices

Possible next decisions.

Examples:

* Pick a number
* Place a queen
* Choose a character
* Move in a direction

---

### 3. Constraints

Rules that determine validity.

Examples:

* No duplicate elements
* Queens cannot attack each other
* Sum must not exceed target

---

### 4. Goal Condition

When a complete valid solution is found.

Examples:

* All queens placed
* Target sum reached
* Path completed

## Basic Execution Flow

```text
Start
 ├── Choice A
 │    ├── Choice A1
 │    ├── Choice A2
 │    └── Dead End → Backtrack
 │
 ├── Choice B
 │    ├── Choice B1
 │    └── Solution
 │
 └── Choice C
```

## Role of Backtracking in Algorithm Design
The primary role of backtracking in algorithm design is to reduce the effective search space of exhaustive search methods. Unlike brute force algorithms, which generate and test all possible candidate solutions, backtracking eliminates infeasible candidates as early as possible. This pruning significantly improves practical performance, even though the worst-case time complexity may remain exponential.

Backtracking's role is to make exhaustive search practical by eliminating impossible choices as early as possible.

Backtracking is especially useful when:
- The problem has strong constraints that can be checked incrementally.
- The number of valid solutions is small compared to the total number of possibilities.
- A complete enumeration of feasible solutions is required.

Backtracking incrementally builds a solution step by step:
1. Choose a candidate option
2. Check if the partial solution is still valid
3. If valid → continue
4. If invalid → undo the choice (backtrack) and try another option

Many problems have:
- Huge combinational spaces
- Strong constraints
- Few valid solutions

Backtracking allows algorithms to:
- Avoid exploring impossible paths
- Enforce constraints early
- Find solutions without checking every combination

So its role is: Reduce exponential search by early rejection

This makes it fundamentally different from:
- Pure brute force → explores all possibilities blindly
- Greedy → commits permanently to local choices
- Dynamic Programming → stores and reuses subproblem results

Backtracking explores only feasible paths.

- DP answers **how many / how optimal**.
- Backtracking answers **is there a valid one / list them all**.

**Backtracking Is Not Always Enough** as it's:
- Still exponential in the worst case
- Does not reuse results
- May revisit equivalent states

That's why:
- Memoization + backtracking → DP
- Heuristics + backtracking → CSP solvers

## Relationship to Brute Force Algorithms
Backtracking can be viewed as an optimized form of brute force search. Both approaches explore the solution space exhaustively; however, brute force evaluates all candidates without discrimination, while backtracking abandons partial solutions that violate constraints before they are fully constructed.

Thus, backtracking improves brute force by incorporating early feasibility checks, preventing unnecessary exploration of invalid branches of the search tree.

| Aspect             | Brute Force       | Backtracking              |
| ------------------ | ----------------- | ------------------------- |
| Explores           | All possibilities | Only feasible ones        |
| Pruning            | None              | Yes                       |
| Typical structure  | Nested loops      | Recursion + undo          |
| Efficiency         | Worst possible    | Often dramatically better |
| Use of constraints | Late              | Early                     |

Backtracking is brute force with intelligence.

## Role of Recursion in Backtracking
Backtracking algorithms are most naturally expressed using recursion, where each recursive call represents a decision at a particular level of the solution construction process. The recursion stack implicitly stores the sequence of choices made so far.

When a recursive call returns, the algorithm automatically restores the previous state, allowing alternative decisions to be explored. This recursive structure aligns closely with a depth-first traversal of a conceptual search tree.

Backtracking almost always uses recursion because:
- Each recursive call represents a decision level
- The call stack naturally stores:
  * Current partial solution
  * Previous states
- Returning from recursion = undoing a choice

Conceptually:
- Recursion = decision tree traversal
- Backtracking = DFS with pruning

## Position Among Algorithmic Strategies
Backtracking occupies a distinct position among algorithmic strategies:
- Brute force explores all possibilities without pruning.
- Backtracking explores only feasible portions of the search space.
- Dynamic programming avoids repeated computation by storing and reusing subproblem results.

While backtracking focuses on feasibility and constraint satisfaction, dynamic programming is primarily concerned with optimality and overlapping subproblems.

Backtracking sits between brute force and dynamic programming:
```
Brute Force
     ↓ (add pruning)
Backtracking
     ↓ (add memoization)
Dynamic Programming
```
- Backtracking focuses on validity constraints
- DP focuses on optimality and overlapping subproblems

They solve different problem classes.

## Applications of Backtracking
Backtracking is commonly applied to:
- Constraint satisfaction problems (e.g., N-Queens, Sudoku, graph coloring)
- Combinatorial generation problems (e.g., permutations, combinations, subsets(with constraints))
- Search problems (e.g., maze traversal, word search)
- Decision problems where the existence of a solution must be determined i.e Is there any valid solution?

## Limitations of Backtracking
Despite its advantages, backtracking does not guarantee polynomial-time performance. In the worst case, the algorithm may still explore an exponential number of states. Additionally, backtracking does not inherently avoid recomputation of equivalent subproblems, which limits its efficiency compared to dynamic programming in problems with overlapping substructures.

## Examples
### Example: Subset Sum
{/*TODO consider moving this at end of document*/}
### Example1: N-Queens backtracking
<details>

<summary>**How do you place eight queens on the board such that no queens attack each other?**</summary>

Refernces: https://code.energy/8-queens-problem/

**Problem Statement**

In Chess pieces move on an 8 × 8 board, attacking enemy pieces. The queen is the most powerful piece: it can attack pieces that occupy its row, its column, or its diagonals. 

The N-Queens problem asks whether it is possible to place N queens on an N × N chessboard such that:
- No two queens share the same row
- No two queens share the same column
- No two queens share the same diagonal
    
The objective is to find one or all valid arrangements satisfying these constraints.

Try finding a solution manually: you'll see it's not trivial. Figure shows one of the ways queens can be peacefully positioned.

![The letmost queen attacks other queens. Moving her up, none of the queens attack each other](/docs/assets/software-systems/data-structures-and-algorithms/algorithms/approaches-strategies/n-queen-1.png)

**the brute force way**

As eight queens can be placed in the chessboard in over four billion ways. Solving this problem the brute force way inspecting all these possibilities is sloppy. Imagine the first two queens are placed on the board attacking each other: regardless where the next queens are placed, a solution isn't possible. Yet, a brute force approach would waste time with all these doomed queen placements.

see software-systems/algorithms-data-structures/algorithms/concepts/approaches-strategies/brute-force-strategy-problems#brute-force-strategy-problems-n-queens

Searching only viable queen placements is more efficient. The
first queen can be placed anywhere. Viable placements for the next queens are limited by already placed queens: a queen cannot be
placed in the attacking range of another queen. Placing queens
following this rule, we're likely to get a board where it's impossible
to place an additional queen before all eight queens are placed:

![Placing a queen limits viable places for the next queens](/docs/assets/software-systems/data-structures-and-algorithms/algorithms/approaches-strategies/n-queen-placing-a-queen-limits-viable-places-for-the-next-queens.png)

This can only mean the last queen was placed incorrectly. So we
backtrack: we roll back the previous placement and continue the
search. That's the essence of the backtracking strategy: keep on
placing queens in valid positions. Once we get stuck, roll back
the placement of the last queen and carry on. The process can be
streamlined using recursion:
```
function queens(board)
  if board.has_ͧ_queens
    return board
  for each position in board.unattacked_positions
    board.place_queen(position)
      solution ← queens(board)
    if solution
      return solution
    board.remove_queen(position)
return False
```
If the board isn't already solved, it loops through all viable positions for the next queen. It uses recursion to check if placing a queen in each of these positions gives a solution. Here's how the
process works:

![Backtracking in the 8 Queens Puzzle](/docs/assets/software-systems/data-structures-and-algorithms/algorithms/approaches-strategies/n-queen-backtracking-in-the-n-queens-puzzle.png)

The problem is solved by placing queens row by row, ensuring that each partial placement is valid before proceeding.
At each row:
- Attempt to place a queen in each column
- Check whether the placement conflicts with previously placed queens
- If a conflict occurs, abandon the placement and try the next column
- If no column is valid, backtrack to the previous row

**Step-by-Step Walkthrough (N = 4)**

<table>
<tr>
<td valign='top'>
<strong>Step 1: Start with an empty board</strong><br />
No queens are placed.
<pre>
. . . .
. . . .
. . . .
. . . .
</pre>
</td>
<td valign='top'>
<strong>Step 2: Place a queen in Row 0</strong><br />
Try columns from left to right.<br />
Place at (Row 0, Column 0) — valid.
<pre>
Q - - -
| \ . .
| . \ .
| . . \
</pre>
</td>
<td valign='top'>
<strong>Step 3: Place a queen in Row 1</strong><br />
- Column 0 → same column conflict ❌<br />
- Column 1 → diagonal conflict ❌<br />
- Column 2 → valid ✔<br />
Place at (1, 2).
<pre>
Q - | /
| \ Q -
| / \ .
| . | \
</pre>
</td>
</tr>
<tr>
<td valign='top'>
<strong>Step 4: Place a queen in Row 2</strong><br />
- Column 0 → same column conflict ❌<br />
- Column 1 → diagonal conflict ❌<br />
- Column 2 → same column conflict ❌<br />
- Column 3 → diagonal conflict ❌<br />
No valid column → dead end<br />
Backtrack: remove queen from (1, 2).
</td>
<td valign='top'>
<strong>Step 5: Continue Row 1</strong><br />
Try next column:<br />
- Column 3 → valid ✔<br />
Place at (1, 3).
<pre>
Q - - |
| \ - Q
| . \ |
| . . \
</pre>
</td>
<td valign='top'>
<strong>Step 6: Retry Row 2</strong><br />
- Column 0 → same column conflict ❌<br />
- Column 1 → valid ✔<br />
Place at (2, 1).
<pre>
Q - - |
| \ - Q
| Q \ |
| | \ \
</pre>
</td>
</tr>
<tr>
<td valign='top'>
<strong>Step 7: Place a queen in Row 3</strong><br />
Q - - |
| \ - Q
| | \ |
| Q - \
- Column 0 → same column conflict ❌<br />
- Column 1 → same column conflict ❌<br />
- Column 2 → diagonal conflict ❌<br />
- Column 3 → same column conflict ❌<br />
No valid column → dead end<br />
Backtrack: remove queen from (2, 1).
</td>
<td valign='top'>
<strong>Step 8: Continue Row 2</strong><br />
- Column 2 → diagonal conflict ❌<br />
- Column 3 → same column conflict ❌<br />
No valid position → backtrack further<br />
Remove queen from (1, 3).
Q - - |
| \ - Q
| . \ |
| . . \
</td>
<td valign='top'>
<strong>Step 9: Backtrack to Row 0</strong><br />
Remove queen from (0, 0).<br />
Try next column.<br />
Place queen at (0, 1).
<pre>
. Q . .
. . . .
. . . .
. . . .
</pre>
</td>
</tr>
<tr>
<td valign='top'>
<strong>Step 10: Repeat the process</strong><br />
Proceed row by row again, applying the same checks.<br />
Eventually, the algorithm finds a valid solution:
<pre>
. Q . .
. . . Q
Q . . .
. . Q .
</pre>
Coordinates:<br />
- (0,1)<br />
- (1,3)<br />
- (2,0)<br />
- (3,2)
</td>
<td></td>
<td></td>
</tr>
</table>

<Tabs groupId='languages'> <TabItem value='c' label='C'>

```c
#include <stdio.h>
#include <stdlib.h>

int isSafe(int board[], int row, int col) {
    for (int i = 0; i < row; i++)
        if (board[i] == col ||
            abs(board[i] - col) == row - i)
            return 0;
    return 1;
}

void printBoard(int board[], int n) {
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++)
            printf(board[i] == j ? 'Q ' : '. ');
        printf('\n');
    }
    printf('\n');
}

void solve(int board[], int row, int n) {
    if (row == n) {
        printBoard(board, n);
        return;
    }
    for (int col = 0; col < n; col++) {
        if (isSafe(board, row, col)) {
            board[row] = col;
            solve(board, row + 1, n);
        }
    }
}

int main() {
    int n = 4;
    int board[n];
    solve(board, 0, n);
    return 0;
}
```

</TabItem> <TabItem value='cpp' label='C++'>

```cpp
#include <iostream>
#include <vector>
using namespace std;

bool isSafe(vector<int>& board, int row, int col) {
    for (int i = 0; i < row; i++)
        if (board[i] == col ||
            abs(board[i] - col) == row - i)
            return false;
    return true;
}

void printBoard(vector<int>& board) {
    int n = board.size();
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < n; j++)
            cout << (board[i] == j ? 'Q ' : '. ');
        cout << endl;
    }
    cout << endl;
}

void solve(vector<int>& board, int row) {
    int n = board.size();
    if (row == n) {
        printBoard(board);
        return;
    }
    for (int col = 0; col < n; col++) {
        if (isSafe(board, row, col)) {
            board[row] = col;
            solve(board, row + 1);
        }
    }
}

int main() {
    int n = 4;
    vector<int> board(n);
    solve(board, 0);
    return 0;
}
```

</TabItem> <TabItem value='java' label='Java'>

```java
public class NQueensBacktracking {

    static boolean isSafe(int[] board, int row, int col) {
        for (int i = 0; i < row; i++)
            if (board[i] == col ||
                Math.abs(board[i] - col) == row - i)
                return false;
        return true;
    }

    static void printBoard(int[] board) {
        int n = board.length;
        for (int i = 0; i < n; i++) {
            for (int j = 0; j < n; j++)
                System.out.print(board[i] == j ? 'Q ' : '. ');
            System.out.println();
        }
        System.out.println();
    }

    static void solve(int[] board, int row) {
        int n = board.length;
        if (row == n) {
            printBoard(board);
            return;
        }
        for (int col = 0; col < n; col++) {
            if (isSafe(board, row, col)) {
                board[row] = col;
                solve(board, row + 1);
            }
        }
    }

    public static void main(String[] args) {
        int n = 4;
        int[] board = new int[n];
        solve(board, 0);
    }
}
```
</TabItem> <TabItem value='python' label='Python'>

```python
def is_safe(board, row, col):
    for i in range(row):
        if board[i] == col or abs(board[i] - col) == row - i:
            return False
    return True

def print_board(board):
    n = len(board)
    for i in range(n):
        for j in range(n):
            print('Q' if board[i] == j else '.', end=' ')
        print()
    print()

def solve(board, row):
    n = len(board)
    if row == n:
        print_board(board)
        return
    for col in range(n):
        if is_safe(board, row, col):
            board[row] = col
            solve(board, row + 1)

n = 4
board = [0] * n
solve(board, 0)
```

</TabItem> <TabItem value='rust' label='Rust'>

```rust
fn is_safe(board: &Vec<usize>, row: usize, col: usize) -> bool {
    for i in 0..row {
        if board[i] == col ||
           (board[i] as i32 - col as i32).abs() == (row as i32 - i as i32)
        {
            return false;
        }
    }
    true
}

fn print_board(board: &Vec<usize>) {
    let n = board.len();
    for i in 0..n {
        for j in 0..n {
            if board[i] == j {
                print!('Q ');
            } else {
                print!('. ');
            }
        }
        println!();
    }
    println!();
}

fn solve(board: &mut Vec<usize>, row: usize) {
    let n = board.len();
    if row == n {
        print_board(board);
        return;
    }
    for col in 0..n {
        if is_safe(board, row, col) {
            board[row] = col;
            solve(board, row + 1);
        }
    }
}

fn main() {
    let n = 4;
    let mut board = vec![0; n];
    solve(&mut board, 0);
}
```
</TabItem> </Tabs>

**Key Observations**
- The algorithm explores a decision tree
- Invalid branches are pruned early
- Backtracking avoids testing all 44=256 possibilities
- Only feasible partial solutions are expanded

**Complexity**
- Worst-case time complexity: O(N!)
- Space complexity: O(N) (recursion stack + board state)

**Conceptual Summary**

The N-Queens problem demonstrates how backtracking systematically explores a combinatorial search space by making choices, detecting conflicts early, and undoing decisions when necessary. This makes backtracking a powerful strategy for constraint satisfaction problems.


</details>
